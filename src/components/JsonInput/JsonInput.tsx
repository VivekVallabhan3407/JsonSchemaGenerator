import React from "react";

const JsonInput = () => {
  return (
    <div>
      <h3>JSON Input</h3>
      <textarea placeholder="Paste your JSON here..." />
    </div>
  );
};

export default JsonInput;
